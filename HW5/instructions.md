Write concise code.
If there are multiple sets of data, they should use different shades of the same color when possible.
Make sure any comments, code, and variable names are clear, concise, and useful. It should look pleasant for a human to read without too many extra characters used
Plots should be stored relative to the python file.

All plotting parameters are defined in `plot_params.yaml'